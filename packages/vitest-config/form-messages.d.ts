export function shownMessages(container: HTMLElement): string[];

export function isMessageShown(container: HTMLElement, text: string): boolean;

export function isMessageHeld(container: HTMLElement, text: string): boolean;
