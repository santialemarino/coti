# API base URL. Reach the API server-side only: it declares no CORS, and the webapp
# answers on its own subdomain in production. See docs/technical/deployment.md.
NEXT_PUBLIC_API_URL=http://localhost:8000
