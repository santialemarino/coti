# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
.next/
out/
build
dist
bin/

# JavaScript / TypeScript
*.tsbuildinfo
next-env.d.ts
coverage

# Go
*.exe
*.test
*.out

# Local object storage — what the local storage adapter writes
apps/api/.storage/

# Environment and secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.pem

# Tooling
.turbo
.vercel
.idea/

# OS
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Claude Code / Codex — agent plans are ephemeral working docs (never commit)
.claude/plans/
.claude/settings.json
# A worktree here is a second copy of the repo, which every repo-wide tool would then walk into
.claude/worktrees/

# Internal docs — plans, costs, architecture, phase specs, decisions
docs/internal/

# Local artifacts
screenshots/
.playwright-cli/
.artifacts/
